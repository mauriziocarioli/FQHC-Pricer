package gov.cms.fqhc_pricer;

import java.lang.Number;
import java.lang.Double;

rule "Total Claim Payment"
	dialect "mvel"
	salience 0
	when
		$claim : Claim( )
		$totalPayment : Double( ) from accumulate ( Line( $linePayment : linePayment != null ),
			sum($linePayment)) 
	then
		modify( $claim ) {
				setTotalClaimPayment( $totalPayment )
		}
end
